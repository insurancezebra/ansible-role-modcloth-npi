# vim:filetype=upstart
# {{ ansible_managed }}

description "New Relic {{ item.key }} Plugin Daemon"

start on filesystem or runlevel [2345]
stop on runlevel [!2345]

respawn
respawn limit 3 30

script
  npi start {{ item.key }} --foreground
end script
